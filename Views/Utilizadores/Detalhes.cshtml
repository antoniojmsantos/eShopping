@model TP_PWEB.Models.ApplicationUser

@{
    ViewBag.Title = "Detalhes";
}

<br />

@if (User.IsInRole("Admin"))
{
<h5>Detalhes Cliente</h5>}

@if (User.IsInRole("Empresa"))
{
<h5>Detalhes Funcionário</h5>}


<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NomeCompleto)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NomeCompleto)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>

    </dl>
</div>
<p>
    
    @if (User.IsInRole("Admin"))
    {
        @Html.ActionLink("Voltar", "ListaClientes", null, new { @class = "btn btn-default" })
    }

    @if (User.IsInRole("Empresa"))
    {
        @Html.ActionLink("Editar", "EditarFuncionario", new { id = Model.Id }, new { @class = "btn btn-warning" }) @:&nbsp;
        @Html.ActionLink("Voltar", "ListaFuncionarios", null, new { @class = "btn btn-defalt" }) 
    }
</p>
